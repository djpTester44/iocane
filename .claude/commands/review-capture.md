Read and execute the workflow defined in `.agent/workflows/review-capture.md`. Follow every step in that file exactly, including the private-method gate that drops `_`-prefixed findings, the routing tag assignment rules from `.agent/rules/ticket-taxonomy.md`, and the append-only constraint on `plans/backlog.yaml`.
