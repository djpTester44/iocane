Read and execute the workflow defined in `.agent/workflows/validate-plan.md`. Follow every step exactly, including the `extract_structure.py` structural skeleton loading in Step 2 (load-once, do not re-read files modified during the run), the per-component CDD compliance scoring, the iterative remediation loop, and the mandatory `IOCANE_VALIDATING=1` guard before writing the `**Plan Validated:** PASS` stamp to `plans/plan.yaml`.
