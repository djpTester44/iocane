Read and execute the workflow defined in `.agent/workflows/io-plan-batch.md`. Follow every step exactly, starting with the Step 0 hard gate (`bash .claude/hooks/pre-invoke-io-plan-batch.sh`) — halt immediately if it exits non-zero. Then proceed with the configuration read and parallel limit enforcement, the dependency resolution and parallelization safety check, the in-memory draft task file generation, the confidence rubric scoring loop (85% threshold, 3-iteration limit), the mandatory human approval gate before any task file is written, and the Accept / Modify / Reject response handling rules.
