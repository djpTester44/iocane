Read and execute the workflow defined in `.agent/workflows/io-orchestrate.md`. Follow every step exactly, including the escalation flag check at startup, the checkpoint status resolution from `.status` files, the backlog gate, the per-checkpoint task file generation format, the confidence rubric scoring (85% threshold, 3 iteration limit), the `run.sh` generation with worktree setup and backgrounded dispatch, and the constraint that this workflow writes files and routes to human but does not execute `run.sh` itself.
