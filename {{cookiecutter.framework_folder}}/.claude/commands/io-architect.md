Read and execute the workflow defined in `.agent/workflows/io-architect.md`. Follow every step exactly, including the roadmap present gate, the plan mode proposal sequence (dependency map → CRC cards → Protocol signatures → Interface Registry), the mandatory human approval gate before any file is written, the contract lock declaration, and the incremental mode rules for handling existing `.pyi` files without silent modification.
